{
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "deploy": "nuxt build && npx wrangler deploy",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "preview:cloudflare": "nuxt build && npx wrangler dev",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.13.0",
    "swiper": "^11.0.0"
  },
  "devDependencies": {
    "@nuxtjs/i18n": "^10.4.0",
    "@nuxtjs/tailwindcss": "^6.12.0",
    "sass": "^1.77.0"
  }
}
